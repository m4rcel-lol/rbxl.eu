<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register — rbxl.eu</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/material.css">
  <link rel="stylesheet" href="/css/app.css">
</head>
<body class="m3-body">
  <%- include('partials/nav') %>
  <main class="m3-main">
    <div class="m3-auth-container">
      <div class="m3-card m3-card-elevated m3-auth-card">
        <div class="m3-card-content">
          <h1 class="m3-headline-medium">Create account</h1>
          <p class="m3-body-medium">Join rbxl.eu and create your profile</p>

          <% if (error) { %>
            <div class="m3-alert m3-alert-error"><%= error %></div>
          <% } %>

          <form method="POST" action="/register" class="m3-form">
            <%- include('partials/csrf') %>
            <div class="m3-field">
              <label class="m3-label" for="username">Username</label>
              <input type="text" id="username" name="username" class="m3-input" required
                pattern="[a-zA-Z0-9_\-]{3,20}" title="3-20 characters, letters, numbers, _ and -"
                autocomplete="username">
            </div>
            <div class="m3-field">
              <label class="m3-label" for="password">Password</label>
              <input type="password" id="password" name="password" class="m3-input" required
                minlength="8" autocomplete="new-password">
            </div>
            <div class="m3-field">
              <label class="m3-label" for="confirmPassword">Confirm Password</label>
              <input type="password" id="confirmPassword" name="confirmPassword" class="m3-input" required
                minlength="8" autocomplete="new-password">
            </div>
            <button type="submit" class="m3-btn m3-btn-filled m3-btn-full">Create Account</button>
          </form>

          <p class="m3-body-medium m3-text-center">
            Already have an account? <a href="/login" class="m3-link">Sign in</a>
          </p>
        </div>
      </div>
    </div>
  </main>
  <script src="/js/app.js"></script>
</body>
</html>
